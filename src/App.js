import { Windows } from "./components";
import { GlobalStyle } from "./globalStyles.elements";

export const App = () => {
  return (
    <div>
      <GlobalStyle />
      <Windows />
    </div>
  );
};
